Script-driven and LLM-driven control are poles of a spectrum, not competing architectures. They compose within a single system — even within a single script. The choice is policy expressed per-agent, per-function, changeable at any point. This spectrum maps directly onto a trust gradient: script-driven gives inspectability before execution; LLM-driven gives observability after execution.