The derivative stack is only climbable when each layer is a composable artifact. Script-driven agents are composable by construction. LLM-driven agents participate in higher-order systems only when wrapped in script-driven orchestration. Simplicity (single agents with tools) outperforms complexity (multi-agent orchestration) — and the scaling path goes through composition, not coordination.